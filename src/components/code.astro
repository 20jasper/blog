---
import type { ComponentProps } from 'astro/types';
import { Code as AstroCode } from 'astro:components';
import theme from '../../orange-theme';

type Props = ComponentProps<typeof AstroCode>;

const { props } = Astro;
---

<AstroCode
	theme={theme}
	wrap
	{...props}
/>
